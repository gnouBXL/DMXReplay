# Standard Flutter build/tooling output -- none of this is checked in, and
# none of it exists yet in this checkout since it's generated by `flutter
# create .`/`flutter pub get`/`flutter build`, none of which have been run
# here (see README.md's "Flutter validation status").
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.iml
*.g.dart
*.freezed.dart

# Platform folders (android/, ios/, linux/, macos/, windows/, web/) are
# intentionally NOT present in this checkout -- generate them with
# `flutter create .` per README.md, then apply the two platform-specific
# permission snippets it documents, before they'd normally be committed.
