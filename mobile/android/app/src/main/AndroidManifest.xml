<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- docs/MOBILE.md SS6: local network access + mDNS discovery
         (CHANGE_WIFI_MULTICAST_STATE lets the app acquire a multicast lock
         for multicast_dns), and the Control API's own plain HTTP/WebSocket
         traffic to a device on the same LAN. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:label="DMXReplay Remote"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic is global, not scoped to specific domains
             (deliberate, not an oversight): the Control API has no HTTPS
             (docs/MOBILE_API.md SS1, a trusted-LAN design), and the device
             address is whatever IP the user enters or mDNS discovers, not
             a fixed domain a network_security_config's per-domain rules
             could usefully scope to. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
